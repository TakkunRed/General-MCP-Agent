[project]
name = "inventory-agent"
version = "0.1.0"
description = "LM Studio + MCP SDK を使った汎用MCPエージェント"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
